enum ScreenStep {
  START = -1,
}

enum QuestionType {
  ARTIST = `artist`,
  GENRE = `genre`,
}

enum FailType {
  TIME = `TIME`,
  MISTAKES = `MISTAKES`,
}

export {ScreenStep, QuestionType, FailType};
